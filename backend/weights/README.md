# This directory holds trained model weights.
# Place your trained model file here as: truelens_model.pth
#
# To train a model:
#   python -m training.train --data_dir ./data --epochs 30
#
# Then copy the best model:
#   cp checkpoints/best_model.pth backend/weights/truelens_model.pth
